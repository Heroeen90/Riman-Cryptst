name: riman_cryptst
description: Next-generation sovereign mathematical hybrid crypto suite leveraging Riemann Zeta Zeros.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cryptography: ^2.5.0
  encrypt: ^5.0.3
  local_auth: ^2.1.8
  flutter_windowmanager: ^0.2.0
  flutter_jailbreak_detection: ^1.2.0
  flutter_secure_storage: ^9.0.0
  sqflite_sqlcipher: ^2.0.0
  battery_plus: ^6.0.0
  device_info_plus: ^10.0.0
  path: ^1.9.0
  path_provider: ^2.1.2
  receive_sharing_intent: ^1.2.0
  flutter_local_notifications: ^17.0.0
  dio: ^5.0.0
  dio_http_formatter: ^3.3.1
  http_certificate_pinning: ^2.0.1
  pointycastle: ^3.7.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
